{
  "name": "yksystems-free-resource-library",
  "version": "1.0.0",
  "private": true,
  "description": "Static launch kit for the YK SYSTEMS Free Resource Library.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:book": "node tools/build-print-assets.mjs",
    "build:supporting": "python3 tools/build-supporting-pdfs.py",
    "verify:links": "node tools/verify-static-links.mjs",
    "verify": "node --check script.js && node --check library.js && node --check review.js && node --check tools/build-print-assets.mjs && npm run build:book && npm run build:supporting && npm run verify:links",
    "serve": "npx --yes serve ."
  }
}
